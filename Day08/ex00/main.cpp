//
// Created by Arborio Herlinda on 8/15/21.
//

#include "Easyfind.hpp"
#include <iostream>
#include <list>
#include <vector>

int main() {
  std::list<int> list(5);
  for (int i = 0; i < static_cast<int>(list.size()); ++i) {
    list.push_back(i);
  }

}
